# gll deployment repository

The website source is maintained in the private repository [Cicadagg/gll-source](https://github.com/Cicadagg/gll-source).

That repository builds the application and publishes only the generated `build` directory to the `gh-pages` branch of this repository.
